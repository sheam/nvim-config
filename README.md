# NVIM CONFIG FILES

## Quick Start
- cd `~/.config`
- `git clone git@github.com:sheam/nvim-config.git nvim`
- note the `nvim` at the end of the clone command, it needs to be cloned into the nvim directory

## Manual Steps
